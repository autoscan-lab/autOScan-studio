# Open Questions

1. Should v1 editing be read-only by default, or editable with save support?
2. Is remote-first (SSH) or local-first grading the default startup mode?
3. Which exact engine interface shape will be frozen for v1?
4. How much similarity/AI detail should appear in main view vs drill-down?
5. What minimum analytics do we need to evaluate TA productivity gains?
